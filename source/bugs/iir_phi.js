function strBufWrite()
{
    "tachyon:static";
    "tachyon:noglobal";

    // If the capacity needs to be extended
    if (true)
    {
        // Copy the characters from the old buffer to the new
        while (true)
        {
            var ch = u16(0);
        }
    }
}

